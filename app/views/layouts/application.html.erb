<!DOCTYPE html>
<html>
  <head>
    <title>KidsMilestones</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://fonts.googleapis.com/css2?family=Kosugi+Maru&display=swap" rel="stylesheet">
  </head>
  <body>
    <header class="header-bar">
      <div class="container header-inner">
        <h1 class="h1"><%= link_to "Kids Milestones", root_path %></h1>
        <nav>
          <% if user_signed_in? %>
            <span style="color:#7b6f69;font-size:.9rem;margin-right:.5rem;">
              ようこそ、
              <%= (current_user.respond_to?(:name) && current_user.name.present?) ? current_user.name : current_user.email %>
            </span>
            <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-secondary" %>
          <% else %>
            <%= link_to "ログイン", new_user_session_path, class: "btn btn-primary" %>
            <%= link_to "新規登録", new_user_registration_path, class: "btn btn-secondary", style: "margin-left:.5rem" %>
          <% end %>
        </nav>
      </div>
    </header>

    <% flash.each do |k, v| %>
      <div class="container" style="margin-top:1rem">
        <div class="flash <%= k %>"><%= v %></div>
      </div>
    <% end %>

    <main class="container" style="padding:1.5rem 0">
      <%= yield %>
    </main>

    <footer class="container footer">© Kids Milestones</footer>
  </body>
</html>